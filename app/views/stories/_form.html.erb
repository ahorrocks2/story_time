<%= render "layouts/errors", :object => @story %>

<div class ="container">
  <%= form_for @story, :html => {:class => "form-horizontal center"} do |f| %>
    <div class="form-group">
      <%= f.label :ftitle, "Title", class: "control-label" %>
        <%= f.text_field :title, class: "form-control" %>
      </div>

    <div class="form-group">
      <%= f.label :fauthor, "Author", class: "control-label" %>
        <%= f.text_field :author, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :ffirst_sentence, "First Sentence", class: "control-label" %>
        <%= f.text_field :first_sentence, class: "form-control" %>
      </div>
    <%= f.submit "Submit", class: "btn btn-default" %>
  <% end %>
</div>
